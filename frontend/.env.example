# Copy to .env. Base URL of the backend API.
# FastAPI (Python) ingestion + read service:
VITE_API_BASE=http://localhost:8000
# ASP.NET Core (.NET) read API — see ../dotnet-api/README.md:
# VITE_API_BASE=http://localhost:5080
